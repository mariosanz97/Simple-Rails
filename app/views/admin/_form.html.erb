<%= form_for @adminc, url: {controller: "admin", action: "create"} do |f| %>
  <%= f.text_field :user, :placeholder => 'User' %>
  <%= f.text_field :password, :placeholder => 'Password'%>
  <%= f.submit 'Create', :class => 'btn btn-warning btn btn-primary' %>
<% end %>